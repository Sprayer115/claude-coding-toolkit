---
description: "Initialize developer session — loads context and shows workflow reference. Invokes the vctk-init-session skill."
allowed-tools: ["Bash", "Read", "Glob", "AskUserQuestion"]
---

This command delegates to the `vctk-init-session` skill.

Use the vctk-init-session skill directly to initialize your session. The skill can be invoked by saying "init session", "load context", or `/vctk-init-session`.
