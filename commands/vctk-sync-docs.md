---
description: "Sync .agent documentation — generate or update architecture docs and SOPs. Invokes the vctk-sync-docs skill."
allowed-tools: ["Bash", "Read", "Write", "Glob", "Grep", "Task", "AskUserQuestion"]
---

This command delegates to the `vctk-sync-docs` skill.

Use the vctk-sync-docs skill directly to synchronize documentation. The skill can be invoked by saying "sync docs", "document the codebase", or `/vctk-sync-docs`.
