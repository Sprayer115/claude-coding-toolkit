---
description: "Tech debt scanner — scans for duplicated code, TODOs, dead code, and inconsistencies. Invokes the vctk-techdebt skill."
allowed-tools: ["Read", "Glob", "Grep", "Bash", "AskUserQuestion"]
---

This command delegates to the `vctk-techdebt` skill.

Use the vctk-techdebt skill directly for a codebase health scan. The skill can be invoked by saying "scan for tech debt", "find duplicated code", or `/vctk-techdebt`.
