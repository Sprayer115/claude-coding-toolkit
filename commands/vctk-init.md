---
description: "Preflight check — verifies VCTK installation and project readiness. Invokes the vctk-init skill."
allowed-tools: ["Bash", "Read", "Glob", "AskUserQuestion"]
---

This command delegates to the `vctk-init` skill.

Use the vctk-init skill directly for a preflight check. The skill can be invoked by saying "check vctk setup", "preflight check", or `/vctk-init`.
